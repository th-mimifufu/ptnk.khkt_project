services:
  ml-api-dev:
    image: khanhlinh02/ml-service:latest
    container_name: ml-api-dev
    ports:
      - "8001:8000"
    env_file:
      - .env.dev
    restart: unless-stopped
    networks:
      - backend-network-dev
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  backend-network-dev:
    driver: bridge
    external: true
    name: backend-network-dev
