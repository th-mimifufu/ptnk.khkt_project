services:
  ml-api-test:
    image: khanhlinh02/ml-service:latest
    container_name: ml-api-test
    ports:
      - "8001:8000"
    env_file:
      - .env.test
    restart: unless-stopped
    networks:
      - app-shared-network-test
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request; urllib.request.urlopen('http://localhost:8000/health')\" || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

networks:
  app-shared-network-test:
    driver: bridge
    external: true
    name: app-shared-network-test
